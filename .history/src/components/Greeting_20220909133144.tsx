import * as React from 'react';

interface IProps {

}
interface IState {

}
const Greeting: React.FC<IProps> = (props) => {
  return (
   <React.Fragment>
   .contain
   </React.Fragment>
  );
};

export default Greeting;
